# No application secrets are required.
# DB is a Cloudflare D1 binding declared in .openai/hosting.json, not an env string.
# Authenticated identity headers are supplied by the Sites gateway.
# Do not configure a client-controlled user ID or production mock authentication.
# TEST_BASE_URL=http://127.0.0.1:8791  # local integration tests only
